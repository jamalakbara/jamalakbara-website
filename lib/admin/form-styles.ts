// Shared admin form className strings. Single source of truth for the
// field/label styling reused across the admin editor forms.
export const input = "field-input w-full";
export const label = "mb-1.5 block text-xs text-[var(--m3)]";
